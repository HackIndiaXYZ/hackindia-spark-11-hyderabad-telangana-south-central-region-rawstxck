// Corresponds to the timeline filters (.filters) in securepush-history.html
export const TimelineFilters = ({ activeFilter, onFilterChange }: { activeFilter?: string, onFilterChange?: (f: string) => void }) => <div />;
